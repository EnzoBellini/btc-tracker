# Railway: defina Root Directory = . (raiz do repositório), não apps/app.
[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build --workspace=@trackion/app"

[deploy]
startCommand = "npm run start --workspace=@trackion/app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
