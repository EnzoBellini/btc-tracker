# URL da API do app (formulário trial). Em dev, o proxy do Vite usa /api → localhost:5000
VITE_API_BASE_URL=https://app.trackion.app

# URL para botões "Começar" / "Entrar" (produto)
VITE_APP_URL=https://app.trackion.app
